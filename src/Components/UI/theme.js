import {
  lightTheme,
  lightContent,
  lightThemeText,
  darkTheme,
  darkContent,
  darkThemeText,
} from "./variables";

export const LightTheme = {
    body: lightTheme,
    inside: lightContent,
    text: lightThemeText,
    filter:''
};

export const DarkTheme = {
    body: darkTheme,
    inside: darkContent,
    text: darkThemeText,
    filter: 'invert(100%)'
}